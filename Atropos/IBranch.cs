namespace Atropos
{
    //interface IBranch<B, T>: IListNode<T>
    //    where B: struct, IBranch<B, T>
    //{
    //    public (Box<B>, Box<B>) Split();
    //    public Box<B> InsertDataAt(Box<B> self, int index, T data);
    //    Box<B> Add(Box<B> self, T value);

    //}
}